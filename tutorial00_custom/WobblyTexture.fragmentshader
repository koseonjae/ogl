#version 330 core

in vec2 uv;
out vec3 fragColor;
uniform sampler2D renderedTextureSampler;
uniform float time;

void main( void )
{
    fragColor = texture( renderedTextureSampler, uv + 0.005*vec2( sin(time+1024.0*uv.x),cos(time+768.0*uv.y)) ).xyz ;
}