#version 330 core

layout(location = 0) in vec2 vertexPosition;
layout(location = 1) in vec2 vertexUV;

out vec2 uv;

void main( void )
{
    vec2 homogenPosition = (vertexPosition - vec2(400, 300)) / vec2(400, 300);
    gl_Position = vec4(homogenPosition, 0, 1);
    uv = vertexUV;
}